apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: dockermule-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dockermule
  template:
    metadata:
      labels:
        app: dockermule
    spec:
      containers:
      - name: dockermule
        image: suprabhatcs/dockermule:92
        ports:
        - containerPort: 8082
      imagePullSecrets:
      - name: regcred
